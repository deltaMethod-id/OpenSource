# OpenSource

Website source code extractor built with Next.js and deployed on Vercel.

## Structure

- `public/index.html` — UI
- `public/css/style.css` — styles
- `public/js/app.js` — client logic
- `app/api/source/route.tsx` — API route

## Development

```bash
npm install
npm run dev
```
