const input=document.getElementById("url");const button=document.getElementById("get");const output=document.getElementById("output");button.addEventListener("click",async()=>{let url=input.value.trim();if(!url)return;try{if(!/^https?:\\/\\//i.test(url))url="https://"+url;button.disabled=true;button.textContent="FETCHING...";const r=await fetch("/api/source",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url})});const data=await r.json();if(!r.ok)throw new Error(data.error||"Request failed");output.textContent=data.source}catch(e){output.textContent="ERROR: "+e.message}finally{button.disabled=false;button.textContent="GET SOURCE"}});